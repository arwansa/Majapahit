const config = {
    server: 'localhost:8888',

    firebaseToken: '[FIREBASE TOKEN FROM CLIENT]',
    device: '[ONESIGNAL player_id FROM CLIENT]',

    firebaseTokenSecondUser: '[FIREBASE TOKEN FROM CLIENT FOR SECOND USER]',
    deviceSecondUser: '[ONESIGNAL player_id FROM CLIENT FOR SECOND USER]'
}
module.exports = config;
